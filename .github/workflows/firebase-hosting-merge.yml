name: Publish ARVUS to Firebase
on:
  push:
    branches: [main]
    paths: ['public/**', 'firebase.json', 'firestore.rules', '.firebaserc', '.github/workflows/firebase-hosting-merge.yml']
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: arvus-firebase-live
  cancel-in-progress: false
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check deployment configuration
        env:
          FIREBASE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ARVUS_BASVURU }}
        run: |
          test -f public/index.html
          test -f public/vendor/three.module.js
          test -f public/media/havacilik-ai.mp4
          if [ -z "$FIREBASE_ACCOUNT" ]; then
            echo 'Firebase bağlantısı kurulmamış. README içindeki GitHub bağlantısı adımını tamamlayın.'
            exit 1
          fi
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ARVUS_BASVURU }}
      - name: Publish Firestore access rules before the panel
        run: npx --yes firebase-tools@13.35.1 deploy --only firestore:rules --project arvus-basvuru --non-interactive
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ARVUS_BASVURU }}
          projectId: arvus-basvuru
          channelId: live
